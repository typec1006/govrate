# 09 セッション切れエラーページ

`/auth/error` — セッション切れや認証エラー時に表示するページ。

## TODO

- [ ] `app/auth/error/page.tsx` 作成
  - [ ] エラーメッセージを表示（「セッションが切れました。再度ログインしてください。」）
  - [ ] トップページへのログインボタンを表示
- [ ] レスポンシブ対応

## 実装メモ

- Supabase Auth のエラー時にこのページへリダイレクトされる
- `searchParams` でエラー種別を受け取り、メッセージを出し分けることも検討する
- ログインボタンは `app/page.tsx` へのリンクにする
