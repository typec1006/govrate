# 今後の改善リスト

実装完了後に対応したい改善・修正の一覧。優先度を目安に管理する。

---

## パフォーマンス

### ナビ押下時の表示遅延

- [x] ページコンテンツを `<Suspense>` + スケルトン UI で囲み、シェルをナビ押下直後に表示する（`/vote`・`/ranking` 両ページ）
- [x] `Header` の認証依存部分（ナビ・ユーザーメニュー）を `<Suspense>` に切り出し、ハードリロード時のブロッキングを解消する
- [x] `React.cache()` で `getUser()` を1リクエスト1回に集約し、`Header` とページで重複する Supabase 往復を削除する

---

## 投票ページ（`/vote`）

### フィルター UI 改善
- [ ] 地域フィルター と 投票状況フィルターの視覚的なグループ分けを明確にする（ラベル追加など）
- [ ] フィルターが複数選択されている時に「リセット」ボタンを表示する
- [ ] スマホでフィルターが折り返すとき、レイアウトが崩れないよう調整
- [ ] フィルター後の件数表示（例：「13件表示中」）

### 投票 UI 改善
- [ ] スコアボタンに星（★）アイコンを使ったデザインに変更
- [ ] 投票後に一瞬フィードバック（アニメーション or トースト通知）を表示

---

## ランキングページ（`/ranking`）

- [ ] 地域フィルター追加（投票ページと同様）
- [ ] スコアを星表示にする
- [ ] 上位3位にメダル（🥇🥈🥉）アイコンを付ける
- [ ] 投票数0の知事を最下部に固定する

---

## ヘッダー

- [ ] スマホ時のナビをハンバーガーメニューに変更
- [ ] ログイン中のユーザー名をドロップダウンに表示する

---

## デザイン全般

- [ ] カラーパレットの整備（ブランドカラーの定義）
- [ ] フォントサイズ・余白のスケール見直し
- [ ] ダークモード対応
- [ ] ローディング状態のスケルトン UI 追加

---

## その他

- [ ] `app/auth/error/page.tsx` のデザイン改善（09チケット実装後）
- [ ] OGP 画像の設定（SNS シェア用）
- [ ] favicon をカスタム画像に変更
