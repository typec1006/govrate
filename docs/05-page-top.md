# 05 トップ / ログインページ

`/` — サービスの入口。未ログイン時はログインを促し、ログイン済みなら投票ページへ。

## TODO

- [x] `app/page.tsx` を実装
  - [x] 未ログイン時：サービス説明 + Googleログインボタンを表示
  - [x] ログイン済み：`/vote` へリダイレクト
- [x] レスポンシブ対応（PC・スマホ）

## 実装メモ

- ログイン状態の確認はServer Componentで行う（`lib/supabase/server.ts`）
- ログイン済みの場合は `redirect('/vote')` を使用する
